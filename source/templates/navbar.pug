link(rel="stylesheet", href="/source/css/navbar.css")
meta(name="viewport", content="width=device-width, initial-scale=1")
div(class="navbar")
	each name, page in pages
		if page == url
			a(href=page, class="selected") #{name}
			title #{name}
		else
			a(href=page) #{name}
